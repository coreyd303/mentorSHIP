<%= form_for @user do |f| %>
  <%= hidden_field_tag :profile_type, "mentor" %>
  <%= f.submit "Mentor", class: "mentor-button" %>
<% end %>

<%= form_for @user do |f| %>
  <%= hidden_field_tag :profile_type, "student" %>
  <%= f.submit "Student", class: "student-button" %>
<% end %>
