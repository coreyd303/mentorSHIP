<div class="modal fade" id="emailModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">

  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <h4 class="modal-title" id="myModalLabel">Email <%= @mentor.name %></h4>
      </div>

      <div class="modal-body">
        <%= form_for(resource, as: resource_name) do |f| %>
          <div class='form-group'>
            <b>To:</b> <%= @mentor.name %><br/>
            <b>From:</b> <%= current_user.student.name %>
          </div>
          <div class="form-group">
            <%= f.text_field :subject, class: "form-control", placeholder: "Subject" %>
            <%= f.hidden_field :mentor, value: @mentor.id %>
            <%= f.hidden_field :student, value: current_user.student.id %>
          </div>
          <div class="form-group">
            <%= f.text_area :body, class: "form-control", placeholder: "Message" %>
          </div>
          <div class="form-group">
            <%= f.submit "Send", class: "btn btn-primary btn-small" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>